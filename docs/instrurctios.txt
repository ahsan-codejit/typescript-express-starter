npm init -y
npm install typescript --save-dev
npm install @types/node --save-dev
tsconfig.json->
npx tsc --init --rootDir src --outDir build \
--esModuleInterop --resolveJsonModule --lib es6 \
--module commonjs --allowJs true --noImplicitAny true

npm install --save-dev ts-node nodemon
add in package.json-> "start:dev": "nodemon",

production build
npm install --save-dev rimraf
add in package.json -> "build": "rimraf ./build && tsc",

mkdir src
touch src/index.ts

npx tsc

Eslint
npm install --save-dev eslint @typescript-eslint/parser @typescript-eslint/eslint-plugin
add .eslintrc
add .eslintignore
add in package.json -> "lint": "eslint . --ext .ts",
adding rules in .eslintrc
adding plugins
npm install --save-dev eslint-plugin-no-loops
extending different base config
npm install eslint-plugin-shopify --save-dev
fixing linted code with eslint -> "lint-and-fix": "eslint . --ext .ts --fix"

#Prettier
npm install --save-dev prettier
add .prettierrc
add in package.json -> "prettier-format": "prettier --config .prettierrc 'src/**/*.ts' --write"
setting prettier on vscode
cmd+shipft+p -> preferences open settings -> json
prettier-watch before commit
npm install --save-dev onchange
in package.json-> "prettier-watch": "onchange 'src/**/*.ts' -- prettier --write {{changed}}"
configure prettier with eslint
npm install --save-dev eslint-config-prettier eslint-plugin-prettier
adjust .eslintrc
